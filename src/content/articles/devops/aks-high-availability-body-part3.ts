import type { Block } from "../../types";

export const bodyPart3: Block[] = [
    {"type": "p", "text": "The recovery path should also produce evidence. Operators need to know whether the part is failed, recovering, healthy, or serving traffic. Otherwise a restart can look like success while the app remains unavailable."},
    {"type": "h2", "id": "capacity", "text": "HA also needs room to absorb a failure"},
    {"type": "p", "text": "A design can have the right number of replicas on paper and still fail when one disappears. The survivors need enough headroom to carry the load, and the cluster needs somewhere to place replacements."},
    {"type": "p", "text": "Suppose a service normally needs four replicas and the cluster has no spare capacity. One node fails. The controller creates the replacement, but the scheduler cannot place it. The desired state is correct. The available service is still smaller."},
    {"type": "p", "text": "Review capacity together with uptime:"},
    {"type": "ul", "items": ["node capacity and node-pool design", "resource requests and limits", "autoscaler behavior and scale-up time", "topology constraints and available domains", "Pod disruption policies", "quota and attachment limits", "image and registry uptime", "capacity of downstream dependencies"]},
    {"type": "p", "text": "Capacity is part of the recovery path. If the system cannot absorb the load while a replacement starts, a brief infrastructure failure can become a customer outage."},
    {"type": "h2", "id": "controllers-and-replicas", "text": "Choose the controller to match the workload"},
    {"type": "p", "text": "Uptime starts with the workload's lifecycle model. A stateless web service usually benefits from a Deployment because Kubernetes can manage a set of interchangeable replicas and roll out replacements. A stateful workload may need stable identity and ordered behavior, which is why StatefulSet exists."},
    {"type": "p", "text": "The point is not that one controller is universally more available. The controller must match what the app needs after a failure. If a replacement can be any equivalent instance, interchangeable replicas simplify recovery. If identity or storage must remain tied to a particular member, the recovery model has to preserve that relationship."},
    {"type": "p", "text": "Replica count also needs to match the traffic and failure model. One replica is effectively cold replacement for a Pod failure: Kubernetes can create another, but there is an interruption while that happens. Several replicas can keep serving while one is repaired, provided the remaining capacity and dependencies are healthy."},
    {"type": "p", "text": "Horizontal Pod Autoscaler can add capacity when demand rises, but scaling is not instantaneous and it does not solve every bottleneck. A slow database, a saturated external API, or a shared network path can remain the limit while the app creates more callers."},
    {"type": "h2", "id": "services-and-traffic", "text": "Services separate instance failure from service identity"},
    {"type": "p", "text": "Pods are replaceable and their addresses change. A Kubernetes Service gives callers a stable service identity and directs traffic to endpoints that Kubernetes considers ready. That matters even when a workload has only one replica because callers do not need to track the Pod's address."},
    {"type": "p", "text": "With several replicas, readiness becomes part of the traffic decision. A Pod that is not ready should not receive new Service traffic. That helps isolate a failed or initializing replica while healthy replicas continue."},
    {"type": "p", "text": "This is a small but important distinction. A Service can hide the identity of individual Pods. It cannot hide a shared app failure. If every ready backend fails for the same database or identity reason, traffic distribution simply spreads the same error."},
    {"type": "h2", "id": "leadership", "text": "Some workloads need a leader, so the leader can become the failure point"},
    {"type": "p", "text": "Not every workload spread across nodes is active-active. Some systems need one member to coordinate work. In those cases, leader election can reduce the risk of a permanent coordinator loss by allowing another healthy member to take the role."},
    {"type": "p", "text": "The design still needs clear failure handling. The system must know when the old leader is gone, prevent two leaders from making conflicting decisions, and allow a new leader to recover the required state. Leader election solves ownership of a role. It does not replace state recovery or durable storage."},
    {"type": "p", "text": "When you review a leader-based part, ask how long a leadership change takes, what happens to work in flight, and whether clients can tolerate the transition. Those answers belong in the uptime objective, not as an afterthought in the controller setup."},
    {"type": "h2", "id": "state-and-storage", "text": "Persistent state changes the recovery boundary"},
    {"type": "p", "text": "Stateless replicas are easier to replace because the useful state lives elsewhere or is recreated. Stateful workloads move part of the recovery problem into the storage and data layer."},
    {"type": "p", "text": "A persistent volume can preserve data across a Pod replacement, but the workload still depends on the behavior of the storage system. Attachment, replication, uptime boundaries, and recovery time all matter. A Pod restart that is quick in the scheduler may still wait for storage or for an app-level recovery process."},
    {"type": "p", "text": "For critical state, ask a stricter question: after this failure, what is the last confirmed state and how do we know? That question links checkpointing, durable writes, backups, and recovery testing."},
];
